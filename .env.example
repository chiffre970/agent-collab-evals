# Copy this file to .env for local development. Never commit .env or real
# credentials. Behavior-changing test inputs belong in committed profiles, not
# in this machine-local file.

# Modal CLI authentication is stored outside this repository by `modal setup`
# in ~/.modal.toml. Use a profile name when a non-default profile is needed.
MODAL_PROFILE=
MODAL_ENVIRONMENT=dev
MODAL_HF_SECRET_NAME=huggingface-secret

# OpenRouter supplies the development preflight model. The real OpenCode
# adapter never inherits this key; it receives only a session token issued by
# the experiment-owned model gateway.
OPENROUTER_API_KEY=

# Optional direct providers for later registered study profiles.
DEEPSEEK_API_KEY=
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=

# Optional headless/CI Modal authentication. Prefer `modal setup` locally.
# MODAL_TOKEN_ID=
# MODAL_TOKEN_SECRET=
